{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git diff --name-only HEAD | grep -qE \"\\.kts?$\" && ./gradlew ktlintFormat --quiet 2>&1 || true"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "git diff --name-only HEAD | grep -qE \"\\.kts?$\" && ./gradlew test --quiet 2>&1 || true"
          }
        ]
      }
    ]
  }
}
